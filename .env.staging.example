# Impulsionando STAGING — project ref aamorcqznimmleafavai
# Prod deny-list: arygtqrdpcdkwnuwsgmm
# Dashboard → this project → Settings → API
# Gitignored. Do not commit.

APP_ENV=staging
NODE_ENV=development

SUPABASE_URL=https://aamorcqznimmleafavai.supabase.co
# MUST be legacy anon JWT (eyJ… ~200+ chars) from staging Dashboard → API → anon public.
# NOT sb_publishable (46 chars) — signInWithPassword / smoke auth will fail.
# NOT prod keys from impulsionando.com.br (project arygtqrdpcdkwnuwsgmm).
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

VITE_SUPABASE_URL=https://aamorcqznimmleafavai.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=aamorcqznimmleafavai

VITE_PUBLIC_SITE_URL=http://localhost:8080
PUBLIC_SITE_URL=http://localhost:8080

# Phase 3 Nest Support API (apps/api) — optional
# Staging CRM support_tickets.company_id is required. Prefer a fixed UUID for
# platform/master company; if unset, API may resolve companies.is_master=true.
# Never commit real values.
# SUPPORT_PLATFORM_COMPANY_ID=
# TanStack strangler: server routes delegate public support create to Nest when set.
PHASE3_API_BASE=https://api.stg.impulsionando.com.br
API_PORT=3100

# Optional — enables `npm run staging:apply:db-patch` (never commit password)
# DATABASE_URL=postgresql://postgres.[ref]:[PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres
# Staff smoke for phase3:smoke:support-live (gitignored values)
# SUPPORT_SMOKE_ACCESS_TOKEN must be a Supabase access_token JWT (eyJ…), NOT anon/service keys.
# Obtain via: sign in on staging → copy session.access_token, or TEST_USER_EMAIL sign-in below.
# TEST_USER_EMAIL=
# TEST_USER_PASSWORD=
# SUPPORT_SMOKE_ACCESS_TOKEN=
