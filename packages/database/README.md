# @impulsionando/database

Placeholder for database access adapters. Depends on domain/contracts per target architecture.

## Canonical redesign SQL drafts

Expand-only draft migrations for the autonomous-marketing canonical model live under [`canonical/`](./canonical/). They are **not** auto-applied and **must not** be applied to staging/production until database gates DB1–DB4 pass. See [`canonical/README.md`](./canonical/README.md).
