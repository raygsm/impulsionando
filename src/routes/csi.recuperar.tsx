import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { KeyRound, Loader2 } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/csi/recuperar")({head:()=>({meta:[{title:"Recuperar acesso — CSI Invest"},{name:"robots",content:"noindex,nofollow"}]}),component:Page});
function Page(){const[email,setEmail]=useState("");const[loading,setLoading]=useState(false);const[msg,setMsg]=useState<string|null>(null);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);try{const{error}=await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:`${window.location.origin}/auth`});if(error)throw error;setMsg("Se o e-mail estiver cadastrado, você receberá as instruções de recuperação.");}catch{setMsg("Não foi possível iniciar a recuperação agora.");}finally{setLoading(false)}}return<main className="min-h-screen bg-[#07111b] text-white grid place-items-center px-5"><div className="w-full max-w-md rounded-[30px] border border-white/10 bg-white/[.045] p-8"><KeyRound className="h-7 w-7 text-amber-300"/><h1 className="mt-5 text-3xl font-semibold">Recuperar acesso</h1><p className="mt-2 text-sm text-slate-400">Informe seu e-mail para receber o fluxo seguro de redefinição.</p><form onSubmit={submit} className="mt-7 space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="voce@email.com" className="w-full rounded-xl border border-white/15 bg-white/[.04] px-4 py-3 text-sm focus:border-amber-300 focus:outline-none"/>{msg&&<p className="text-sm text-slate-300">{msg}</p>}<button disabled={loading} className="inline-flex w-full items-center justify-center gap-2 rounded-full bg-amber-300 px-5 py-3 font-semibold text-slate-950">{loading?<Loader2 className="h-4 w-4 animate-spin"/>:<KeyRound className="h-4 w-4"/>} Enviar recuperação</button></form><Link to="/csi/entrar" className="mt-5 block text-center text-xs text-amber-300">Voltar ao login</Link></div></main>}
