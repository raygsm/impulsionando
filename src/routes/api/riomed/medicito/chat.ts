import { createFileRoute } from "@tanstack/react-router";
import { randomUUID } from "crypto";
import { streamText,type ModelMessage } from "ai";
import { resolveProvider } from "@/lib/impulsionito/providers.server";
import { listConversationHistory,recordInboundMessage,recordOutboundMessage,type InboundLedger } from "@/lib/agents/omnichannel.server";
function sessionId(request:Request){const supplied=request.headers.get("x-riomed-session")?.trim()??"";return /^riomed:[A-Za-z0-9:_-]{8,200}$/.test(supplied)?supplied:`riomed:web:${randomUUID()}`;}function historyToMessages(history:Awaited<ReturnType<typeof listConversationHistory>>):ModelMessage[]{return history.flatMap((m):ModelMessage[]=>{const text=(m.body_text??"").trim();if(!text)return[];if(m.direction==="INBOUND"||m.author_type==="CONTACT")return[{role:"user",content:text}];if(m.direction==="OUTBOUND"&&m.author_type==="AGENT")return[{role:"assistant",content:text}];return[];});}function tracked(source:ReadableStream<string>,done:(t:string)=>Promise<void>){const enc=new TextEncoder();return new ReadableStream<Uint8Array>({async start(c){const r=source.getReader();let full="";try{while(true){const{value,done:d}=await r.read();if(d)break;if(!value)continue;full+=value;c.enqueue(enc.encode(value));}if(full.trim())await done(full);c.close();}catch(e){c.error(e);}finally{try{r.releaseLock();}catch{}}}});}
const SYSTEM=`Você é MEDICITO — SEU CONCIERGE MÉDICO, agente oficial da RioMed e CLIENT_INSTANCE do Impulsionito. Atenda, qualifique e venda consultivamente equipamentos e serviços RioMed. Nunca invente estoque, preço, prazo, fabricante, modelo, SKU, garantia, compatibilidade, condição, certificação ou informação clínica. Nunca faça diagnóstico ou prescrição. Use somente OpenAI na sua chave exclusiva RioMed. Se faltar dado operacional, diga que não está disponível no Core e encaminhe corretamente.`;
export const Route=createFileRoute("/api/riomed/medicito/chat")({server:{handlers:{POST:async({request})=>{let body:any;try{body=await request.json();}catch{return Response.json({error:"invalid_json"},{status:400});}const text=typeof body?.text==="string"?body.text.trim():typeof body?.message==="string"?body.message.trim():"";if(!text)return Response.json({error:"empty_message"},{status:400});if(text.length>12000)return Response.json({error:"message_too_large"},{status:413});let ledger:InboundLedger;try{ledger=await recordInboundMessage({agentKey:"riomed-medicito",channel:"web_chat",provider:"riomed_front",externalUserId:sessionId(request),bodyText:text,endpointAddress:"https://riomed.impulsionando.com.br",metadata:{source:"riomed_medicito_web",pathname:typeof body?.pathname==="string"?body.pathname.slice(0,300):"/"}});}catch{return Response.json({error:"conversation_ledger_unavailable"},{status:503});}let messages:ModelMessage[];try{messages=historyToMessages(await listConversationHistory(ledger.conversation_id,30));}catch{messages=[{role:"user",content:text}];}let resolved;try{resolved=resolveProvider({agentKey:"riomed-medicito",llm:{provider:"openai"},allowFallback:false});}catch(error){console.error("[riomed/medicito] provider unavailable",error);return Response.json({error:"openai_unavailable"},{status:503});}try{const result=streamText({model:resolved.model,system:SYSTEM,messages:messages.slice(-30),temperature:0.2,maxOutputTokens:1200});const stream=tracked(result.textStream as ReadableStream<string>,output=>recordOutboundMessage({conversationId:ledger.conversation_id,bodyText:output,channel:"web_chat",provider:"riomed_front",endpointId:ledger.endpoint_id,status:"SENT",metadata:{provider:resolved.provider,model:resolved.modelId,agent_key:"riomed-medicito",root_agent_key:"impulsionito-core",strict_provider:true}}));return new Response(stream,{headers:{"Content-Type":"text/plain; charset=utf-8","Cache-Control":"no-store","X-Agent-Key":"riomed-medicito","X-Conversation-Id":ledger.conversation_id,"X-LLM-Provider":"openai"}});}catch(error){console.error("[riomed/medicito] stream failed",error);return Response.json({error:"upstream_error"},{status:502});}}}}});
