import { createFileRoute } from '@tanstack/react-router';
import { randomUUID } from 'crypto';
import { streamText, type ModelMessage } from 'ai';
import { resolveProvider } from '@/lib/impulsionito/providers.server';
import { supabaseAdmin } from '@/integrations/supabase/client.server';
import { closeConversationForExternalIdentity, listConversationHistory, recordInboundMessage, recordOutboundMessage } from '@/lib/agents/omnichannel.server';
const BASE_SYSTEM=`Você é Milito — WMP Wagner Miller Produções, cérebro comercial e operacional da WMP. Responda em português do Brasil, objetivamente, com cordialidade e foco em conversão. Qualifique eventos, contratação de DJ, demandas recorrentes de hotéis/empresas, parceiros, agenda pública e suporte. Nunca invente preços, estoque, certificações, cases, disponibilidade ou agenda. Proposta comercial preliminar vem antes do contrato. Não revele custos internos, margens, dados pessoais, tokens ou segredos. Ao concluir pergunte exatamente: "Algo mais em que eu ainda possa ajudar?".`;
const EXPORT_QUESTION='Perfeito. Deseja receber por e-mail uma cópia completa desta conversa, com o número de protocolo do atendimento? Se desejar, responda “sim”. Para sua segurança, antes do envio pediremos apenas um cadastro básico: nome completo, celular e e-mail. Os demais dados são opcionais e poderão ser preenchidos agora ou depois.';
function sessionId(request:Request){const supplied=request.headers.get('x-wmp-session')?.trim()??'';return /^wmp:[A-Za-z0-9:_-]{8,200}$/.test(supplied)?supplied:`wmp:ephemeral:${randomUUID()}`;}
function normalize(text:string){return text.toLocaleLowerCase('pt-BR').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9\s]/g,' ').replace(/\s+/g,' ').trim();}
function toMessages(history:Awaited<ReturnType<typeof listConversationHistory>>):ModelMessage[]{return history.flatMap((m):ModelMessage[]=>{const content=(m.body_text??'').trim();if(!content)return[];if(m.direction==='INBOUND'||m.author_type==='CONTACT')return[{role:'user',content}];if(m.direction==='OUTBOUND'&&m.author_type==='AGENT')return[{role:'assistant',content}];return[];});}
async function getOperationalContext(){const today=new Date().toISOString().slice(0,10);const{data:schedule}=await supabaseAdmin.from('wmp_whereabouts_entries' as never).select('event_date,venue_name,venue_address,start_time,end_time,status,published_at' as never).gte('event_date' as never,today).eq('status' as never,'PUBLISHED').not('published_at' as never,'is',null).order('event_date' as never,{ascending:true}).order('start_time' as never,{ascending:true}).limit(12);const entries=((schedule as unknown as Array<Record<string,unknown>>)??[]).map(item=>({date:item.event_date,venue:item.venue_name,address:item.venue_address,start:item.start_time,end:item.end_time}));return entries.length?`Agenda pública futura validada: ${JSON.stringify(entries)}.`:'Agenda pública futura validada: nenhuma entrada publicada no momento. Não invente local ou data.';}
function isNegativeClosure(text:string){return /^(nao|nao obrigado|nao obrigada|nao valeu|nada|so isso|somente isso|era isso|por enquanto nao|por ora nao|obrigado|obrigada)$/.test(normalize(text));}function isAffirmative(text:string){return /^(sim|sim quero|quero|quero sim|pode|pode sim|claro|ok|okay|exportar|quero exportar|envie|pode enviar)$/.test(normalize(text));}function isNegativeExport(text:string){return /^(nao|nao quero|agora nao|nao obrigado|nao obrigada|dispenso|deixa pra la|deixe para la)$/.test(normalize(text));}
async function fixedResponse(input:{conversationId:string;endpointId:string|null;text:string;metadata?:Record<string,unknown>}){await recordOutboundMessage({conversationId:input.conversationId,bodyText:input.text,channel:'web_chat',provider:'wmp_front',endpointId:input.endpointId,metadata:{agent:'Milito',...(input.metadata??{})}});return new Response(input.text,{headers:{'content-type':'text/plain; charset=utf-8','cache-control':'no-store','x-conversation-id':input.conversationId}});}
export const Route=createFileRoute('/api/wmp/millito/chat')({server:{handlers:{POST:async({request})=>{const body=await request.json().catch(()=>null) as {text?:string}|null;const text=body?.text?.trim()??'';if(!text)return Response.json({error:'empty_message'},{status:400});if(text.length>12000)return Response.json({error:'message_too_large'},{status:413});const externalUserId=sessionId(request);const ledger=await recordInboundMessage({agentKey:'wmp-millito',channel:'web_chat',provider:'wmp_front',externalUserId,bodyText:text,endpointAddress:'https://wmp.impulsionando.com.br',metadata:{source:'wmp_millito_web_chat'}});const rawHistory=await listConversationHistory(ledger.conversation_id,40);const outbound=rawHistory.filter(m=>m.direction==='OUTBOUND'&&m.author_type==='AGENT');const previousAssistant=outbound.length?outbound[outbound.length-1]?.body_text??'':'';if(previousAssistant.includes('Algo mais em que eu ainda possa ajudar?')&&isNegativeClosure(text))return fixedResponse({conversationId:ledger.conversation_id,endpointId:ledger.endpoint_id,text:EXPORT_QUESTION,metadata:{system_event:'export_offered'}});const awaiting=previousAssistant.includes('Deseja receber por e-mail uma cópia completa desta conversa');if(awaiting&&isAffirmative(text)){const closed=await closeConversationForExternalIdentity({agentKey:'wmp-millito',channel:'web_chat',provider:'wmp_front',externalUserId});const registrationUrl=`/wmp/conversa/${encodeURIComponent(closed.protocol)}?token=${encodeURIComponent(closed.accessToken)}`;return fixedResponse({conversationId:closed.conversationId,endpointId:ledger.endpoint_id,text:`Ótimo. Para receber a conversa por e-mail, conclua o cadastro básico.\n\nProtocolo: ${closed.protocol}\nCadastro para envio: ${registrationUrl}`});}if(awaiting&&isNegativeExport(text)){const closed=await closeConversationForExternalIdentity({agentKey:'wmp-millito',channel:'web_chat',provider:'wmp_front',externalUserId});return fixedResponse({conversationId:closed.conversationId,endpointId:ledger.endpoint_id,text:`Sem problema. Atendimento encerrado com sucesso. Protocolo: ${closed.protocol}. Quando precisar, o Milito estará por aqui.`});}const history=toMessages(rawHistory);const operationalContext=await getOperationalContext().catch(()=>'Contexto operacional indisponível; não invente agenda.');let resolved;try{resolved=resolveProvider({agentKey:'wmp-millito',llm:{provider:'openai'},allowFallback:false});}catch(error){console.error('[wmp/millito] provider unavailable',error);return Response.json({error:'provider_unavailable'},{status:503});}const result=streamText({model:resolved.model,system:`${BASE_SYSTEM}\n\n${operationalContext}`,messages:history,temperature:0.25,maxOutputTokens:900});const encoder=new TextEncoder();const stream=new ReadableStream<Uint8Array>({async start(controller){let full='';try{for await(const chunk of result.textStream){full+=chunk;controller.enqueue(encoder.encode(chunk));}if(full.trim())await recordOutboundMessage({conversationId:ledger.conversation_id,bodyText:full,channel:'web_chat',provider:'wmp_front',endpointId:ledger.endpoint_id,metadata:{agent:'Milito',agent_key:'wmp-millito',provider:resolved.provider,model:resolved.modelId}});controller.close();}catch(error){controller.error(error);}}});return new Response(stream,{headers:{'content-type':'text/plain; charset=utf-8','cache-control':'no-store','x-conversation-id':ledger.conversation_id,'x-agent-key':'wmp-millito'}});}}}});
