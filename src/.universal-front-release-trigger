Universal production front release trigger.
Purpose: force a fresh Production Front deployment after VPS disk expansion and cleanup on 2026-08-21.
Requirements: rebuild current main once, publish the canonical Core runtime, evict competing frontend routers, and verify Impulsionando plus every active tenant serves the exact release SHA.
No application runtime behavior is changed by this marker.
