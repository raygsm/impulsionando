name: CSI Legacy Emergency Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy CSI emergency publisher is retired.'
          echo 'Production Front is the only frontend publication authority.'
