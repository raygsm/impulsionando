name: Lint & Validate Workflows

# Valida sintaxe YAML e, principalmente, schema/expressoes do GitHub Actions.
# Regras puramente cosmeticas de EOF/linhas vazias ficam fora do gate para que
# arquivos legados semanticamente validos nao bloqueiem mudancas de infraestrutura.

on:
  push:
    branches: [main]
    paths: ['.github/workflows/**']
  pull_request:
    paths: ['.github/workflows/**']
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Instalar yamllint + actionlint
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y yamllint
          bash <(curl -sSf https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash)
          sudo mv ./actionlint /usr/local/bin/actionlint

      - name: yamllint (sintaxe YAML)
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if [ "$EVENT_NAME" = "pull_request" ]; then
            mapfile -t files < <(git diff --diff-filter=ACMRT --name-only "$BASE_SHA" "$HEAD_SHA" -- '.github/workflows/*.yml')
          else
            mapfile -t files < <(find .github/workflows -maxdepth 1 -type f -name '*.yml' | sort)
          fi
          [ "${#files[@]}" -eq 0 ] || yamllint -d "{extends: relaxed, rules: {line-length: disable, new-line-at-end-of-file: disable, empty-lines: disable}}" "${files[@]}"

      - name: actionlint (schema GitHub Actions)
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if [ "$EVENT_NAME" = "pull_request" ]; then
            mapfile -t files < <(git diff --diff-filter=ACMRT --name-only "$BASE_SHA" "$HEAD_SHA" -- '.github/workflows/*.yml')
          else
            mapfile -t files < <(find .github/workflows -maxdepth 1 -type f -name '*.yml' | sort)
          fi
          [ "${#files[@]}" -eq 0 ] || actionlint -color "${files[@]}"

      - name: Listar workflows detectados
        run: |
          echo "Workflows validos publicados na branch:"
          ls -1 .github/workflows/*.yml
