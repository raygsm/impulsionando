name: CSI Legacy Final Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy CSI final publisher is retired.'
          echo 'Production Front is the only frontend publication authority.'
