name: Colors Legacy Router Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy Colors router fixer is retired.'
          echo 'Production Front is the only frontend publication authority.'
