name: Retired one-shot Core deploy patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "One-shot deploy patch retired; canonical deploy workflow already contains the edge synchronization fix."
