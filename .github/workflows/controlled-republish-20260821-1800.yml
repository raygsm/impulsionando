name: RETIRED - Controlled Republish 2026-08-21 18:00 BRT

# Historical recovery workflow kept as a fail-closed tombstone.
# Production Front is the single authority for the 18:00 BRT snapshot promotion.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical publication authority
        run: |
          echo "::error::RETIRED: auxiliary 18h republish path is disabled."
          echo "Use .github/workflows/production-front.yml only."
          exit 1
