# Módulos da plataforma

Use quando o pedido afeta **um módulo do Core**, não um tenant vertical.

Cada módulo tem: `problemas/` · `novas-funcionalidades/` · `melhorias/`

Se não souber o módulo, use `geral/` ou pergunte ao agente de intake.
